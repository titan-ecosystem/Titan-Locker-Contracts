ETHERSCAN_API_KEY=
ETHERSCAN_API_KEY_BASE=
COINMARKETCAP=
ROBINHOOD_RPC_URL=

# Optional: a single raw private key to deploy from, as an alternative to
# mnemonic.txt. Takes priority over mnemonic.txt if both are set.
# NEVER commit this file with a real value filled in - .env is gitignored,
# only ever edit it locally, never paste a real key into chat/this editor session.
DEPLOYER_PRIVATE_KEY=
